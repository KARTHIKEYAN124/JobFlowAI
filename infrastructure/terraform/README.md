# Terraform deployment boundary

Production infrastructure is intentionally provider-neutral in this portfolio build. Add environment-specific modules here for network, managed PostgreSQL, managed Redis, container services, secret storage, observability, and DNS after choosing AWS, Railway, or Render. Do not commit generated state or secrets.

