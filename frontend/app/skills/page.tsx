import { PageHeading } from "@/components/page-heading";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Progress } from "@/components/ui/progress";
import { skillDemand } from "@/lib/data";
export default function SkillsPage(){return <><PageHeading title="Skills intelligence" description="What German employers request—and where your strongest opportunities to grow are."/><div className="grid gap-6 xl:grid-cols-[1.4fr_1fr]"><Card><CardHeader><CardTitle>Most requested</CardTitle><CardDescription>Across roles matching your preferences</CardDescription></CardHeader><CardContent className="flex flex-col gap-6">{skillDemand.map(item=><div key={item.skill}><div className="mb-2 flex justify-between text-sm"><span className="font-semibold">{item.skill}</span><span className="text-muted-foreground">{item.count} jobs · {item.value}%</span></div><Progress value={item.value}/></div>)}</CardContent></Card><div className="flex flex-col gap-6"><Card><CardHeader><CardTitle>Your gaps</CardTitle><CardDescription>Prioritized by impact on high matches</CardDescription></CardHeader><CardContent className="flex flex-wrap gap-2">{["Kafka · 34%","Terraform · 27%","Kubernetes · 24%","German B2 · 22%"].map((skill,i)=><Badge key={skill} variant={i<2?"warning":"secondary"}>{skill}</Badge>)}</CardContent></Card><Card><CardHeader><CardTitle>Your strengths</CardTitle></CardHeader><CardContent className="flex flex-wrap gap-2">{["Java","Spring Boot","Python","FastAPI","REST","PostgreSQL","Docker","AWS","React"].map(skill=><Badge key={skill} variant="success">{skill}</Badge>)}</CardContent></Card></div></div></>}

